#!/bin/sh
#this is a script to start h3c
#author:monkey

connected=$(ifconfig eth0 | grep RUNNING)
if [ "$connected" ]
then
    echo "abc" | sudo -S ip link set eth0 up
    echo $""
    echo "1" | sudo yah3c
    echo "abc" | sudo -S ip link set eth0 up
    echo "1" | sudo yah3c
fi
