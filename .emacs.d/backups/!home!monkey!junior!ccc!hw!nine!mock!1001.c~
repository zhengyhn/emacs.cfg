#include <stdio.h>

#define N 101

int main()
{
	int a[N];
	int i,j;

	while(scanf("%d",&a[0]) != EOF){
		i = 1;
		while(scanf("%d",&a[i]) && a[i] != 9999){
			if(a[i] - a[i - 1] == 1){
				printf("%d %d",a[i - 1],a[i]);
				i++;
				while(scanf("%d",&a[i]) && a[i] != 9999){
					if(a[i] - a[i - 1] == 1){
						printf(" %d",a[i]);
						i++;
					}else{
						break;
					}
				}
				if(a[i] == 9999){
					break;
				}
			}
			i++;
		}
		
	
